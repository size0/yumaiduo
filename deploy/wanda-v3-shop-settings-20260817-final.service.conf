[Service]
# This filename sorts after the existing auto-reply override, so the running
# process and its working directory use the same release.
WorkingDirectory=/opt/wanda-v3-backend/releases/v3-shop-settings-20260817
