[Service]
# V3 only: recognize a seat-map first, send the recognized facts, then ask the
# separate realtime quote endpoint. This override intentionally does not alter
# preview-key environment variables.
WorkingDirectory=/opt/wanda-preview-plugin/releases/v3-two-stage-preview-messages-20260817-233338
ExecStart=
ExecStart=/usr/bin/node /opt/wanda-preview-plugin/releases/v3-two-stage-preview-messages-20260817-233338/index.mjs
