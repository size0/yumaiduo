# 当前生产知识库快照与V4迁移审核

> 快照时间：2026-08-25  
> 生产运行源：`/opt/wanda-v3-backend/data/knowledge_base.json`  
> 当前数量：10条，均为`source=manual`、`status=approved`、`enabled=true`。  
> 本文件只是V4迁移审核快照，不会自动同步生产数据，也不是运行时事实源。

## 1. 与人工会话总结的关系

- `RECENT_HUMAN_CONVERSATION_SUMMARY.md`：最近500条人工对照轮次的统计、问题模式和候选策略。
- 本文件：当前生产已经启用的10条知识及迁移判断。
- 两者不能合并成运行时知识文件：前者是未经人工逐条审核的证据报告，后者是现有人工规则快照。
- V4最终启用的知识应另有版本化数据存储，并经过`draft → replayed → approved → enabled`流程。

## 2. 迁移结论

| 标题 | 分类/场景 | V4建议 | 原因 |
|---|---|---|---|
| 实时核价与金额规则 | quote/general | 不按原文迁移 | 动态公式必须放在确定性报价配置和工具内，不应由模型知识掌管 |
| 核价成功与无法核价回复 | reply/general | 拆分后迁移 | 回复模板与工具失败映射应独立；原文“默认不展示总价”不适用于完整报价 |
| 价格对比与纯文字咨询 | reply/general | 修订后迁移 | 可保留回复策略，但不得复述截图金额或重新生成交易结论 |
| 会话与出票边界 | reply/general | 修订后迁移 | “确认位置张数后进入人工出票”缺少报价、订单、付款门禁 |
| 已知场次下只追问唯一缺失分店 | reply/intake | 保留 | 与当前最小追问和候选引用规则一致 |
| 位置描述只记录偏好 | reply/intake | 保留 | 与当前文字座位证据边界一致 |
| 待付款订单缺张数时只追问张数 | reply/order | 修订后迁移 | 必须优先读取关联订单和事件时点报价，不能只看聊天字段 |
| 接受意向必须依赖有效权威报价 | reply/quote_followup | 保留 | 与确认门禁一致 |
| 报单价后主动确认张数 | reply/quote_followup | 保留策略、调度独立 | 知识可指导当轮表达，定时补问必须由持久化调度器实现 |
| 选座识图与买家意图 | vision/general | 必须重写 | 不能把非W+区域全部折叠为普通，也不能把任意标记直接作为W+或张数证据 |

## 3. 当前生产条目

### 3.1 实时核价与金额规则

- 分类：`quote`
- 场景：`general`
- 当前状态：已审核、已启用
- V4迁移：**不按原文迁移**

当前内容：

> 价格、库存和座位可售状态必须由万达实时接口确认，不能根据座位图颜色或买家截图金额判断。普通座=实时会员价+1元/张。W+或未官方选座但有座位标记：实时会员价≤60元时按座位原价-2.9元/张；>60元时直接报实时会员价。混合或不同座位类型必须按实际座位类型分别核价，不得按买家截图金额拆价。指定座位只有被实时接口确认不可售时，才能提示换相邻座。原始单价计算后先四舍五入保留1位小数，再用该单价×张数计算总价；例如42.86+1=43.86，最终43.9元/张，2张总价87.8元。

审核意见：

- “价格、库存来自实时接口”可保留为安全原则。
- 具体调整值、阈值和轮整必须由后端报价配置决定，不能注入模型作为金额规则。
- V4只接收`quote_realtime`返回的最终权威金额，不复制公式。

### 3.2 核价成功与无法核价回复

- 分类：`reply`
- 场景：`general`
- 当前状态：已审核、已启用
- V4迁移：**拆分后迁移**

当前内容：

> 核价成功固定回复：影片：{影片}；影院：{影院全名}；{单价} 元/张。影院必须使用万达实时匹配的标准全称，不能使用模型返回的省略名称；不展示买家截图价格；默认不展示总价。图片有座位标记但无法确认位置或张数时回复：看到您圈的位置了，请问需要几张？确认后我按您标记的位置帮您核价。影院无法唯一定位时回复：请问是哪个城市的万达广场店？确认影院后，我按您标记的位置帮您实时核价。只有实时接口确认不可售后，才能回复：您说的 {座位} 已实时核对，目前不可售。麻烦换一下相邻位置，我马上帮您核价。

审核意见：

- 完整报价必须展示张数和总价；只有张数未知的合法单价试价可以仅展示单价并询问张数。
- 成功、缺张数、影院不唯一和座位不可核验应拆成独立确定性模板。

### 3.3 价格对比与纯文字咨询

- 分类：`reply`
- 场景：`general`
- 当前状态：已审核、已启用
- V4迁移：**修订后迁移**

当前内容：

> 买家说“比这还贵”“位置都是一样价格吗”并补发个人优惠截图时，归类为价格对比追问，不把第二张图当成新选座任务，不重新走圈选确认，不使用买家优惠价重新报价。固定回复：看到了，您选的是 {座位}。图里 {买家优惠价} 是您账号优惠后的价格，不同账号优惠会不一样；我们这边按实时会员价核算，具体以实时核到的价格为准。买家问“怎么买”时回复：在的，您把影院、影片场次和想要的座位截图发我，我帮您实时核价。问“万达会员锁座的都能买吗？”时回复：可以帮您核，但要以具体影院、场次和座位的实时库存为准。您发一下选座截图或影院、影片场次，我先帮您确认能不能锁到。不得承诺所有IMAX都能买或一定能锁到。

审核意见：

- 已有有效报价时应读取`read_active_quote`，不得重新识图或重复试价。
- 不应向模型提供或要求模型复述`{买家优惠价}`。
- 纯文字W+查询可以先解析身份并调用只读实时座位工具，不应一律要求截图。

### 3.4 会话与出票边界

- 分类：`reply`
- 场景：`general`
- 当前状态：已审核、已启用
- V4迁移：**修订后迁移**

当前内容：

> 同一买家连续发图片、影院、城市、张数、座位文字时，应合并成一轮处理并只回复一次；后续补充“对，两张”“一共4张”“某城市万达”应补全上一轮任务并重新核价。人工客服回复后AI应暂停，不能抢答或重复报价。价格对比、订单、售后上下文中的新图片不能无条件当作新选座任务；同一轮兜底确认最多发送一次。AI只负责识图、理解意图、实时核价和确认位置张数；不能承诺已经锁座、出票、发货、退款。买家确认位置和张数后进入人工出票，AI不自动出票。

审核意见：

- 补充张数不一定重新临时试价；已有合法单价快照时应确定性计算总价。
- 人工出票必须发生在有效报价、确认、订单关联和付款核验之后。
- 人工接管和源消息覆盖规则保留。

### 3.5 已知场次下只追问唯一缺失分店

- 分类：`reply`
- 场景：`intake`
- 当前状态：已审核、已启用
- V4迁移：**保留**

当前内容：

> 买家已经给出城市、影院片段、影片、日期和时间并询问W+座位时，先保留全部已知事实；影院仍有多个官方候选时只追问具体分店。买家随后说“第二个”等序号指代时，应结合上一轮候选继续解析，不得重复询问已经明确的影片、日期、时间或张数。座位可用性必须由只读官方工具确认，不能凭文字判断。

### 3.6 位置描述只记录偏好

- 分类：`reply`
- 场景：`intake`
- 当前状态：已审核、已启用
- V4迁移：**保留**

当前内容：

> 买家说靠后、靠中间、连座等位置要求时，将其理解为人工出票偏好并保留，不把文字偏好改写成官方选座，也不据此声称有票、锁座或重新报价。

### 3.7 待付款订单缺张数时只追问张数

- 分类：`reply`
- 场景：`order`
- 当前状态：已审核、已启用
- V4迁移：**修订后迁移**

当前内容：

> 买家已拍下待付款但当前会话缺少明确张数时，只追问需要几张；张数明确后由确定性订单链读取关联订单和权威报价决定下一步，AI不得生成金额、执行改价或声称已经改价。

审核意见：

- 必须先调用`read_linked_order`并读取事件时点报价投影。
- 如果权威订单数量已经明确，不应继续追问买家。

### 3.8 接受意向必须依赖有效权威报价

- 分类：`reply`
- 场景：`quote_followup`
- 当前状态：已审核、已启用
- V4迁移：**保留**

当前内容：

> 买家表达接受、可以、要了等意向时，先读取当前权威报价状态。只有存在已送达且未过期报价时才能进入确认和下单引导；没有权威报价时不得自行给金额、承诺库存或引导付款。

### 3.9 报单价后主动确认张数

- 分类：`reply`
- 场景：`quote_followup`
- 当前状态：已审核、已启用
- V4迁移：**保留策略，调度独立实现**

当前内容：

> 当系统权威事实确认已经向买家说明单张价格、但买家仍未提供购票张数时，优先在当前回复末尾直接询问“需要几张呢？”。如果当前任务是经确定性定时器触发的一次性无回复跟进，也只能发送一次简短张数询问；买家已说明张数、人工或插件已经询问、出现新消息、人工接管、关联订单、付款、报价失效或会话结束时不得重复追问。该策略只用于推动买家补充数量，不得把人工文字价格当作交易金额证据，也不得声明库存、座位可售、锁座、改价、付款或履约结论。

### 3.10 选座识图与买家意图

- 分类：`vision`
- 场景：`general`
- 当前状态：已审核、已启用
- V4迁移：**必须重写**

当前内容：

> 只有页面底部或订单区明确显示已选座位卡片、具体“X排Y座”时，才算官方已选；官方已选座的座位数即张数，不再追问。手绘圈、线、箭头标记在座位图内视为买家想要的位置；未官方选座但有标记时按 W+ 会员座位处理。圈在场次时间或影院文字上只用于锁定场次/影院，不等同选座。小范围红圈、蓝圈、箭头同样必须识别。买家文字明确说“10排13、14、15”“6排8 9”时，文字座位号也是购买意图；“2张/一共4张/这两个位置”用于确认张数；图片和文字必须合并理解。业务座位类型只有 W+ 和普通：除明确 W+ 外，特惠、普通、优选及其他区域全部归普通座。截图中的原价、优惠价、券后价、合计价只用于理解截图，绝不能作为报价、座位类型、会员价或最终价格依据。

审核意见：

- 官方已选座仍需逐座命中实时座位图。
- 手绘标记必须经过真假门禁，只作为人工履约提示，不能自动证明W+、张数或库存。
- 文字座位只是偏好，不能成为官方选座。
- 普通、优选、特惠等实时类型不能一律折叠；混合座位必须按`area_code + zone_type`分组试价。
- “这两个位置”必须结合上下文判断，不能无条件覆盖官方张数。

## 4. V4建议的运行时拆分

现有10条知识不应原样放入一个Prompt。建议拆为：

```text
确定性交易配置
  - 报价调整值、阈值、轮整、成本上下限

确定性回复模板
  - 报价成功、缺张数、场次不唯一、释放失败、改价成功

Agent回复知识
  - 最小追问、服务范围、位置偏好、接受意向、一次性张数跟进

视觉识别规则
  - 官方选座证据、手绘标记排除、文字与图片事实融合边界

工具门禁
  - 实时座位、有效报价、订单状态、付款和人工接管
```

只有“Agent回复知识”进入语义检索和模型上下文；其他内容由相应确定性组件掌管。
