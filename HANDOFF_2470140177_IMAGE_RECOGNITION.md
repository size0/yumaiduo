# 买家 2470140177 图片识别问题交接

更新时间：2026-08-28

## 1. 当前生产版本

- V4：`2.4.80`
- 插件：`wanda-seat-autoquote`，版本 `2.2.49`
- V4 生产目标：`/opt/wanda-v4/releases/v4-2.4.80-20260827-211900`
- 插件生产目标：`/opt/wanda-seat-autoquote/releases/plugin-2.2.49-persistent-ui-jobs-20260827-221105`
- `wanda-v4.service`、`wanda-seat-autoquote.service` 均正常。
- 当前会话策略：`ai_reply_enabled=false`，但图片识别和确定性报价流程仍开启。

## 2. 买家会话

- 买家：`2470140177`
- tenant/shop：`107 / 2313315754`
- chat：`65878366553`
- 首张图片事件：`fadaef690b0fd87548c109edd1f7c102271cbdf4e480a590fb2a34ba687ba689`
- 图片远端消息 ID：`4272042859642.PNM`
- 随后文字：`六排中间两个`
- 文字事件：`fa4c93fb5b3a5c808e1824ed426e3be872d5560ddb046ab0bbcd04eb51324914`

## 3. 图片为什么没有完成识别

### 已确认事实

生产日志显示：

1. 图片已成功下载，不是图片 URL、网络下载或图片格式拦截：
   - 文件大小：`128112` bytes
   - Content-Type：`image/webp`
2. Qwen 视觉快速识别请求已发出并返回 HTTP `200`：
   - 模型：`qwen3.5-flash-2026-02-23`
   - 耗时：约 `3576ms`
3. 快速结果未通过可靠性检查，因此进入二次解释流程。
4. GPT-5.5 解释器请求也返回 HTTP `200`：
   - 耗时：约 `26481ms`
5. 解释器返回内容没有通过后端 `MovieImageInfo` 的 JSON/Pydantic schema 校验，最终日志为：
   - `vision_failed code=provider_schema_invalid`

因此，**根因不是图片没下载下来，也不是 Qwen 接口 HTTP 失败，而是模型返回结果在业务层结构化校验阶段失败**。

### 目前无法进一步确认的细节

生产环境没有持久化模型原始响应，这是有意的敏感数据最小化设计。因此目前无法严谨区分：

- Qwen 快速结果是“低置信度/关键字段为空”；还是
- Qwen 快速结果本身已发生 schema 校验失败；以及
- GPT-5.5 最终响应具体是哪个字段、时间格式、额外字段、座位数量或 JSON 结构不符合 schema。

能确定的是：**最终解释结果没有成功转换成 `MovieImageInfo`，所以没有进入万达官方影院/场次/座位核价流程，也没有生成报价记录。**

## 4. 买家实际收到的消息

图片事件并非没有回复。后端生成并执行了 `send_message`：

- command：`cmd-6786e55fa202adf4be66c4a2db883cd70ebb228e`
- 执行状态：`succeeded`
- 平台消息 ID：`4268348380413.PNM`

实际文案：

```text
这张截图暂时无法完成识别：图片暂时无法读取或识别
请重新发送清晰、完整的当前场次与选座截图。
```

第二条“六排中间两个”没有回复，是另一条逻辑：它只有排座信息，且前一张图没有形成有效识别上下文；当前 `ai_reply_enabled=false` 又禁止普通自由文本兜底回复，所以系统返回 `event_processed` 且没有动作。

## 5. 当前安全结论

- 未重放历史事件。
- 未补发历史消息。
- 未操作该买家订单。
- 未修改生产配置。
- 未发现图片下载失败或插件命令发送失败。
- 没有报价记录是因为识别 schema 失败发生在报价前。

## 6. 后续建议

1. 增加结构化识别失败的可观测性：只记录脱敏的校验失败类别和字段名，不记录完整图片或模型原文。
2. 为 `provider_schema_invalid` 增加自动安全重试/标准化解析，但不能放宽到允许未经校验的影院、场次、座位或金额进入报价流程。
3. 增加“图片识别失败后，买家仅补充排座”时的确定性模板回复，避免 AI 关闭时静默。
4. 任何生产代码部署、生产模板保存或历史会话补发操作，都必须取得新的明确授权。
5. 不要重放本买家历史事件来验证修复；应使用脱敏本地 fixture 或新自然事件验证。
