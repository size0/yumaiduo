# 万达电影票识别与报价限制

> 用途：供 V4 本地 Agent、工具适配器和测试使用。
>
> 权威来源：`E:\鱼麦多\V3\BUSINESS_RULES.md`。  
> 架构参考：`E:\鱼麦多\V3\plugin-auto-reply-work\ARCHITECTURE.md` 第 5～6 节。  
> 本文是开发摘要，不替代交易规则和后端工具门禁。

## 1. 总原则

- 模型可以理解上下文、选择工具、追问和组织回复，但不能生成或修改价格、优惠、库存、订单、付款、释放或履约事实。
- 识图只生成候选图片事实；影院、场次、座位、优惠和订单事实必须继续经过官方目录、官方场次、实时座位和交易工具验证。
- V4 不得在本地复制价格公式，也不得把截图价格、买家文字价格、人工聊天报价或历史报价作为本店报价来源。
- 工具失败、证据冲突、结果未知或释放无法确认时必须失败关闭，不能让模型猜测。

## 2. 图片识别限制

### 2.1 可以识别

- 平台/页面类型；
- 影院、影片、日期、开场时间、影厅、语言格式；
- 清晰的官方已选座卡片及规范座位号；
- 页面是否存在确认选座按钮、已选座卡片；
- 手绘标记是否可能存在及其大致区域，但只作为人工出票提示。

### 2.2 不能从图片直接认定

- 实时库存或当前仍可购买；
- 本店成本、会员价或最终报价；
- 圈选座位的准确座位号；
- 圈选数量等于购票张数；
- 文字座位或地图颜色等于官方已选座；
- 图片中的价格属于本店可用优惠。

### 2.3 证据等级

1. **官方已选座卡片**：可作为座位和张数证据，但每个座位仍须在万达实时座位图逐座命中。
2. **买家明确文字张数**：可作为张数证据。
3. **手绘圈、红点、绿点和位置标记**：只作为人工出票指令，不是官方选座、库存、价格或张数证据。
4. **文字座位号**：只作沟通和人工参考，不得自动升级为官方选座。
5. **截图价格**：不得作为报价来源；仅允许在同张数、同座区、高置信度时用于单向买家低价保护。

### 2.4 手绘标记门禁

- 只有 `hand_drawn_circle.exists=true` 不够。
- 还必须存在有界座位估计，或能唯一定位到明确单排且不是缩略图、导航、预览、顶部图例等系统区域。
- 猫眼/淘票票缩略图视口框、已售图标、W+图标、座位边框和内置颜色不得当作手绘标记。
- 手绘估计数量无论是多少，都不得自动成为购票张数。

### 2.5 多图与上下文

- 30 秒内最多联合两张近期图片。
- 最新合格选座页负责座位/区域证据；较早影院详情页只能补充不冲突的城市、影院、地址提示、影片、日期、场次和影厅。
- 不得跨图混合座位或价格。
- 买家明确城市后，城市成为影院/场次匹配硬边界。
- 图片后补充城市、影院、影片、日期、时间或张数时，应与最近识图草稿融合，不要求无意义地重发图片。

### 2.6 识图缓存

- 纯图片事实可以按图片内容 SHA-256、视觉 Prompt 版本和模型配置跨会话复用。
- 视觉模型请求不得携带买家文字、会话 ID、订单或付款上下文。
- 缓存不得包含图片 URL、买家身份、报价、库存、场次匹配结果、临时订单、付款或会话状态。
- 模型或 Prompt 版本变化时缓存必须自动失效。
- 复用识图结果后，实时场次、座位、优惠、临时试价、取消和释放仍必须重新执行。

## 3. 影院与场次匹配限制

- 影院必须进入官方影院目录匹配。
- 影片、绝对日期、开场时间、影厅和影院需要形成唯一联合场次。
- 买家明确城市后禁止跨城市候选。
- 相对日期按 `Asia/Shanghai` 解析。
- 标题截断、别名和品牌格式词只允许受控归一化；不能凭相似名称猜影院。
- 影院或场次不唯一时返回缺失项/候选，不能进入实时报价。
- 场次解析只能读取官方目录和场次，不得顺带锁座或创建临时订单。

## 4. 报价来源限制

最终报价只能使用：

1. 当前万达实时座位图中的实时原价；
2. 同轮临时单座试价 `available-offers` 返回的唯一可用 W+会员专享优惠；
3. 后台当前报价规则。

禁止使用：

- 截图标价；
- 买家文字中的价格；
- 模型计算或猜测价格；
- 人工聊天报价；
- 历史报价；
- `wPlusActivity.price` 作为最终优惠价；
- `channelFee` 擅自加入报价或利润公式。

`channelFee` 只能作为有界诊断证据。

## 5. W+与单座试价限制

- `allotSeat.totalPayPrice` 是被试代表座位的**单张应付价**，不得除以买家张数。
- 会员优惠读取接口只允许 `quantity=1`。
- 同一实时座位类型只选择一个真实代表座位试价。
- 必须从 `available-offers` 中取得唯一、可用且名称明确的 W+会员专享活动。
- 优惠缺失、不唯一、金额异常或账号证据不一致时整轮失败。
- `pricing_account_ref` 必须由独立服务端HMAC密钥生成；缺失合法引用时不得确认报价或改价。

## 6. 三种报价路径

### 6.1 官方已选座

- 每个座位必须逐座命中实时座位图。
- 任一座位当前不可用时返回 `official_selection_unverifiable`，不得降级为区域试价。
- 按 `area_code + zone_type` 分组。
- 每组只锁一个真实代表座位进行单座试价。
- 各组单价映射回具体座位后逐座求和。
- 混合座区不得把总优惠平均成统一单价。
- 任一组试价、释放或账号归因失败，整轮报价失败。

### 6.2 有位置标记但无官方选座

- 标记只作为人工出票指令。
- 买家明确张数时，按张数从同场实时可用 W+区域选择试价座位。
- 张数未知时只试 1 张并询问张数。
- 不展示或猜测具体座位号。

### 6.3 无官方选座也无标记

- 仍只允许从同场实时可用 W+区域试价。
- 张数未知时只读单张价格并询问张数。
- 不得因为地图中普通/特惠/优选区域存在优惠就替代W+区域试价。

## 7. 临时订单与释放限制

创建临时试价必须同时满足：

- `code=0`；
- `bizCode=0`；
- 存在 `orderId`；
- `orderStatus=40`；
- `lockSeatTime>=0`。

取消后必须满足：

- `orderStatus=60`；
- `lockSeatTime=-1`。

释放验证：

1. 取消后立即读取实时座位；
2. 2 秒后重读；
3. 再等待 5 秒重读；
4. 仍未恢复则本轮报价失败；
5. 可在约15秒、30秒后台只读复核，但后续恢复也不能把原失败报价改成成功。

其他限制：

- 同一场次临时试价必须串行。
- 后台释放复核期间不得换账号或继续创建临时订单。
- 创建结果未知时不得重复创建。
- `temporary_lock_release_unverified` 只表示本轮特定试价座位未及时确认恢复，不代表整场售罄。
- 临时试价只用于读取优惠，不为买家保留座位。

## 8. 价格边界

- 最终价格不得低于权威会员成本下限。
- 最终价格不得高于对应座位实时原价上限。
- W+区域按照后台阈值、调整值和十分位轮整规则确定性计算。
- 普通/优选/特惠官方座位使用对应代表座位的单张会员价加后台普通区调整值。
- 会员价下限高于实时原价上限、无法同时满足成本和原价边界时返回 `quote_price_conflict`。
- 模型只能复述工具返回的最终金额，不能自行加减、平均或重算。

## 9. 报价有效性与复用

有效报价至少需要：

- 实时核价成功；
- 张数明确；
- 单价和总价一致；
- 报价规则版本明确；
- `pricing_account_ref` 合法；
- 完整报价成功发送；
- 已登记平台消息ID；
- 报价仍未过期；
- 场次、张数和座位范围没有变化。

已有有效报价后：

- 买家追问“多少钱/这个呢/会员能优惠吗”必须读取 `read_active_quote`；
- 不得重新识图或重复临时试价；
- 买家改变张数、场次或发送新的官方选座图时，旧报价立即失效；
- 新官方选座报价必须明确替换旧区域报价并要求重新确认。

## 10. V4接入要求

V4调用后端工具时应把后端结果当作权威Observation，不在本地复制交易逻辑。

推荐处理方式：

```text
模型自主选择工具
  → V4调用受鉴权后端
  → 后端执行影院/场次/座位/价格/释放门禁
  → 返回 {status, code, summary, facts, missing, retryable}
  → Tool Result回注模型
  → 模型继续调用工具、追问或自然回复
```

V4必须保留：

- 租户和会话身份绑定；
- 工具调用ID与幂等键；
- 写工具串行；
- 未知写结果禁止自动重试；
- 发送前交易事实校验；
- 人工接管和新买家消息覆盖门禁；
- 完整Agent、Tool和Outbox审计日志。

V4不应复制：

- 影院目录；
- 场次唯一匹配算法；
- W+价格公式；
- 临时订单状态判断；
- 释放轮询；
- 报价有效性和订单改价门禁。

这些继续由V3后端和插件权威工具负责。
