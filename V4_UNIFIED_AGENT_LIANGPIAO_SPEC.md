# V4 万达电影票：固定流程、Agent、知识库与良票 API 统一总规范

> 版本：V4 统一基准稿
> 用途：作为产品、后端、插件运行时、知识库、Agent 提示词和测试的唯一对照文档。
> 状态：需求与接口契约汇总；本文本身不代表代码已经全部实现。

## 1. 为什么需要这份文档

此前的规则、提示词、截图说明和良票接口文档分散在不同文件中，容易出现以下问题：

- 线上规则先回复，Agent 没有机会结合上下文处理；
- 同一句“可以”在不同阶段被当成不同意图；
- 买家纠正座位后被强制要求重新发截图；
- 特惠票失败后没有清晰的换一口价流程；
- `FAILED` 订单被错误地再次调用退款接口；
- 知识库里的旧文案仍然要求人工出票，而当前目标是由 Agent 受控执行；
- 提示词、硬编码、接口状态和实际订单状态互相覆盖。

本规范的优先级固定为：

1. 良票 API 的真实状态和接口约束；
2. 后端订单状态机、价格和出票结果；
3. 本规范中确认的固定流程；
4. Agent 对非固定问题的理解、追问和工具编排；
5. 普通知识库文案。

任何低优先级内容都不能覆盖真实接口返回、价格、订单状态或出票结果。

## 2. 总体目标与责任边界

### 2.1 目标

系统采用“固定交易骨架 + Agent 处理其余问题”的模式：

- 交易骨架必须稳定、可审计、可重试、可回滚；
- Agent 可以直接回复买家，也可以在权限范围内调用工具；
- Agent 不通过猜测生成影院、场次、座位、价格、订单状态或出票结果；
- 所有价格、订单和出票动作必须由后端工具完成并返回结构化结果；
- 规则只保留必要的安全闸门和固定流程，不再堆叠大量“猜买家意图”的硬编码。

### 2.2 Agent 可以做什么

- 读取当前会话、订单状态、最近有效报价和已识别的影院/影片/场次/座位；
- 判断缺少哪个业务字段，并主动追问一个或一组必要字段；
- 理解自然语言座位表达并转成标准座位列表；
- 识别买家是在咨询、补充信息、纠正座位、确认报价、询问失败原因还是请求售后；
- 调用查询工具、识别工具、报价工具、订单工具和人工任务工具；
- 根据工具返回的结构化参数组织买家可读的回复；
- 在特惠出票失败时，询问是否切换一口价，并在买家明确同意后重新报价和下单；
- 在无法安全完成时说明正在处理/需要人工，不猜测、不承诺。

### 2.3 Agent 不可以做什么

- 自己计算、修改或编造价格；
- 自己声称已出票、已退款、已锁座或已发货；
- 用任意历史文字推断本次购票数量；
- 在买家没有明确数量或座位数量无法确定时直接创建订单；
- 在 `FAILED` 订单上调用 `/api/v1/order/refund`；
- 绕过后端工具直接写订单、改价、锁座、出票、退款或发货状态；
- 暴露良票上游错误码、供应商名称、签名、密钥或内部 URL；
- 把买家一句孤立的“可以/确认/好的”当作永久授权。

### 2.4 规则保留范围

规则只负责四类事情：

1. 安全闸门：权限、幂等、金额、状态、敏感信息和人工接管；
2. 固定交易步骤：识别、报价、拍下、改价、付款、出票、发货/失败；
3. 接口状态映射：把真实状态转换成可理解的买家回复；
4. 明确的时效与频控：报价有效期、催单频率、消息合并和重试。

其他问法、追问顺序、语气和上下文衔接交给 Agent。

## 3. 买家完整流程（模拟买家视角）

### 3.1 咨询开始

买家可能说：

- “北京万达广场看某某电影，18:55，两张多少钱？”
- “多少钱一张？”
- “我想看今晚的电影。”
- “这个能买么？”

Agent 的处理顺序：

1. 读取会话中已有的影院、城市、影片、日期、场次、座位和订单；
2. 只补问缺失且会阻塞下一步的字段；
3. 如果买家只问价格，说明需要选座截图或完整的影院/影片/场次信息，并引导发送；
4. 如果已有完整文字信息，允许直接进入场次查询，不强制截图；
5. 如果买家发送截图，识别消息中的全部图片，合并同一购票上下文；非选座图片作为普通咨询处理；
6. 不因收到普通文字就抢先发送交易模板；应先让 Agent 结合上下文判断是否需要回复。

示例回复：

> 可以的，把美团、猫眼或淘票票的选座截图发我，我按你选的座位查询优惠价；也可以直接告诉我城市、影院、影片、日期和场次。

### 3.2 影院、影片、场次补全

若工具返回缺失：

- 缺城市：询问所在城市或地区；
- 缺影院：列出匹配影院让买家确认；
- 缺影片：询问影片名称；
- 缺日期：询问观影日期；
- 缺场次：查询该影院影片的可售场次，让买家选择；
- 场次已过期或距离开场不足限制：说明无法按当前渠道购票，建议其他场次。

Agent 必须以工具返回为准。不能因为买家说“万达”就擅自选择某一家影院，也不能把影院名相似的结果直接当成确认结果。

### 3.3 识别选座截图

1. 先调用 `/api/v1/recognize/seat-shot`；图片较大或接口要求异步时使用 `/recognize/seat-shot/async`；
2. 识别结果只作为候选，不能直接当成最终事实；
3. 需要时用 `/recognize/confirm` 确认 `CANDIDATE`；
4. 影院、影片、日期、场次和座位最终必须能被良票查询或订单工具接受；
5. 图片无法识别时，说明需要清晰、完整的当前场次选座图，不要把“识别失败”说成“没有座位”。

如果买家随后纠正座位：

- `9排的13 14`、`9排的13和14`、`9排13 14`、`第9排13、14`统一为：`[{row:9,col:13},{row:9,col:14}]`；
- 已有影院、影片、日期、场次和原截图上下文时，直接按新座位调用 `/api/v1/order/preflight` 重新报价；
- 只有上下文缺失、座位无法映射或场次已变化时，才要求重新发截图；
- 不得重复发送“请重新发完整截图”的固定回复来替代真实的座位纠正处理。

### 3.4 报价

报价前必须完成：

1. 影院、影片、日期、场次存在且未过期；
2. 座位已标准化，或 W+ 区域报价已经明确数量；
3. 通过 `/api/v1/order/preflight` 获取最新价格和有效期；
4. 记录 `quoteId/quoteNo`、`priceMode`、`ticketMode`、总价、单价、座位和过期时间。

报价展示规则：

- 默认只展示推荐方案和实际金额；
- 不向买家暴露 `LIMIT/FIXED`、供应商、内部成本等内部标签；
- 只有特惠失败后切换时，才说明“备用渠道/一口价”，同时展示真实的一口价金额；
- Agent 可以改写表达，但不能改动工具返回的金额、座位和有效期；
- 买家说贵时，按固定规则告知不能议价，不进入价格讨价还价循环。

### 3.5 W+ 区域报价

- 没有具体座位：先返回会员座位单价，并询问需要几张；
- 买家说“中间会员座位”：按会员座位单价查询，并告知将优先安排中间区域；
- 有座位标记：告知出票会按标记位置由人工/出票侧处理，不重复报价；
- 没有标记：引导买家标记座位并重新发送；若买家拒绝，明确无法保证位置并创建人工任务；
- 会员区不可用：告知当前场次没有可用会员座位，建议换场次或重新选座，不自动降级到其他票种；
- W+ 区域没有明确票数且座位也不能确定数量时，必须询问“需要几张”。

注意：数量应来自当前消息、当前已确认座位或有效报价，不能从任意历史消息抽取。

### 3.6 买家确认与拍下

买家收到报价后可能说：

- “确认报价，按这个下单”；
- “我要2张”；
- “可以买2张”；
- “好的/可以/嗯嗯/收到”。

处理规则：

- 明确数量和明确下单语义同时存在，才可进入订单流程；
- “好的/可以/收到/嗯嗯”本身不构成交易确认；没有票数时继续问票数；
- 但在系统刚刚明确询问“是否切换一口价/备用渠道”后，单独“可以/确认”可作为该次切换同意；这只是针对紧邻问题的局部授权；
- 过期报价不能直接下单，必须重新预报价并再次确认；
- 不重复要求买家发送已经存在且仍有效的截图。

### 3.7 闲鱼拍下、改价与付款

1. 买家在闲鱼拍下商品；
2. 系统接收 `order.created`，优先处理订单事件；
3. 从有效报价中选择最早的有效未付款订单；重复或不匹配订单不覆盖已确认订单；
4. 只有当当前报价与拍下数量一致时，才能执行改价；
5. 改价金额来自有效报价或 `/order/preflight`，不能由 Agent 自行计算；
6. 改价成功后通知买家付款；提醒金额、有效期和不需要提供密码/验证码；
7. 买家未付款且报价过期，关闭/作废当前交易并要求重新报价；
8. 买家在改价前已付款时，按现有退款/人工策略处理，不自动继续错误出票。

### 3.8 付款后出票

1. 接收 `order.paid`；
2. 核验该闲鱼订单与报价、数量、金额的绑定；
3. 调用 `/api/v1/order/create`，必须传 `ticketMode`、`priceMode`、`tel`、`outOrderNo`，必要时传 `maxPrice`；
4. 订单创建成功只代表进入出票链路或资金冻结，不代表已出票；
5. 状态以回调和 `/api/v1/order/detail` 为准；
6. `TICKETED` 但票列表为空时，先用 `pickupUrl` 或等待 `ticket.updated`，不能说“出票失败”；
7. 出票成功后将票码/取票信息发给买家并发货；
8. 买家发送“获取票码”但尚未出票时，固定回复“您还没有出票成功的订单~”，同时 Agent 可补充当前真实状态。

### 3.9 特惠票出票失败：切换一口价

仅当以下条件同时成立时进入切换流程：

- 原订单是良票特惠 `STANDARD + LIMIT`；
- 收到明确的 `order.failed`；
- 订单未被买家提交平台退款；
- 店铺级“一口价失败后可切换”开关已开启；
- 店铺存在独立的一口价规则且能够成功预报价。

处理顺序：

1. 收到 `order.failed`，记录 `refundAmount`、`lossAmount`、`failReason`；
2. 不向买家暴露上游错误码或供应商细节；
3. 先向闲鱼原订单发起关闭/退款 command，告知买家特惠渠道出票失败正在处理；原订单权威状态确认为 `closed` 后，再询问是否换一口价继续出票；
4. `order.failed` 已按良票语义释放冻结资金，不能再调用 `/api/v1/order/refund`；
5. 买家明确同意后，复用原影院、影片、场次和座位上下文，重新调用 `/order/preflight`，`priceMode=FIXED`；
6. 返回一口价真实金额、总价、有效期，要求买家重新确认；
7. 买家重新拍下正确商品后，在固定窗口内自动匹配；
8. 新订单付款后调用 `/order/create`，不得跨模式自动回退到其他渠道；关闭结果 `unknown` 时只读对账，不重复取消。

买家说“不要换了/不买了”时，取消切换流程，恢复原有失败/退款处理。买家只犹豫或没有回复时不自动下单；等待店铺配置的超时时间（默认30分钟）后结束切换上下文。若买家已提交平台退款，立即停止切换。

一口价第二次仍失败时，不再尝试第三种渠道，按失败/退款固定流程处理，并静默创建人工任务。

## 4. 良票 API 调用矩阵

| 业务阶段 | 必要接口 | 调用条件 | 关键约束 |
|---|---|---|---|
| 连通性 | `/api/v1/ping` | 服务启动或健康检查 | 不携带业务数据 |
| 城市/区域 | `/city/list`、`/city/locate`、`/region/list` | 城市或区域缺失 | 结果需买家确认 |
| 影院 | `/brand/list`、`/cinema/list`、`/cinema/detail` | 需要匹配影院 | 不凭简称猜影院 |
| 影片 | `/movie/list`、`/movie/detail` | 影片缺失或需校验 | 以接口 ID 为准 |
| 场次 | `/show/dates`、`/show/list`、`/show/detail` | 日期/场次缺失或过期 | `show/detail` 可强制刷新 |
| 座位图 | `/seat/list` | 需要座位详情或纠正座位 | 注意 `graphReliable` 和 `maxSeatsPerOrder` |
| 截图识别 | `/recognize/seat-shot` 或 async 版本 | 收到选座图 | 识别结果是候选，不是最终事实 |
| 精确报价 | `/order/preflight` | 座位/数量确定后 | 不创建订单、不冻结资金；报价有有效期 |
| 创建订单 | `/order/create` | 闲鱼订单已拍下且金额匹配 | 必须幂等；冻结成功不等于出票成功 |
| 查单 | `/order/detail` | 回调缺失、状态冲突或人工核验 | 返回是权威订单状态 |
| 对账 | `/order/list` | 启动恢复、定时对账 | 不能只看回调最后一条 |
| 取消 | `/order/cancel` | `TICKETING` 且允许取消 | `cancelled=false` 仍需等待结果 |
| 催单 | `/order/urge` | 非万达、`TICKETING`、距上次至少10分钟 | `accepted=true` 仅表示受理 |
| 退款 | `/order/refund` | 仅 `TICKETED` 且未开场 | 异步；不能用于 `FAILED` |
| 退款查询 | `/order/refund/detail` | 已发起退款或状态不明 | 以退款结果回调为准 |

所有请求使用签名认证。工具层负责签名、超时、重试和脱敏，Agent 不接触密钥。

## 5. 价格规则与店铺配置

### 5.1 渠道选择

- 默认报价：开场前40分钟及以上优先 `STANDARD + LIMIT`；
- 距离开场不足40分钟：按店铺配置优先 `FAST + FIXED`；
- 特惠失败切换：固定使用同影院、同影片、同场次、同座位的 `STANDARD + FIXED`；
- 不允许 provider 在接口层无条件跨模式自动回退；切换必须由 Agent 获得买家同意后发生。

### 5.2 一口价规则

后台新增独立的 `liangpiao_fixed_rules`，不能与特惠规则共用一套覆盖关系：

- 影院/影片/场次/座位区域/日期条件；
- 单张价格或加价规则；
- 最低价、最高价和有效期；
- 可用时段与库存条件；
- 无匹配规则时禁止自动切换，转人工；
- 迁移旧配置时复制到特惠和一口价两套，并标记“待复核”，避免静默改变价格。

前端配置至少有“特惠规则”和“一口价规则”两个明确入口。Agent 只读取后端计算出的 `totalAmount`，不得读取内部成本后自行运算。

### 5.3 区域报价策略

W+ 区域默认使用 `areaQuoteStrategy=AVERAGE`。最终价格仍以 `/order/preflight` 返回为准。

## 6. Agent 行为协议

### 6.1 每轮处理顺序

1. 读取最新消息、订单状态和有效报价；
2. 合并350毫秒窗口内的连续买家消息；新消息到达时取消旧 Agent 任务；
3. 判断是否命中固定交易事件；
4. 若不是固定事件，调用 Agent 生成结构化动作计划；
5. 查询动作可连续执行；每轮最多一个写动作（创建订单、改价、取消、退款、催单等）；
6. 写动作成功后重新读取状态，再生成买家回复；
7. 工具失败不猜测，回复处理中并创建人工任务；
8. 发送失败只重试发送，不重跑业务动作。

### 6.2 结构化输出

Agent 内部输出必须类似：

```json
{
  "reply": "给买家的文本",
  "action": "ask|quote|confirm|create_order|change_price|ticket_status|switch_fixed|human",
  "tool_calls": [],
  "required_fields": [],
  "confidence": 0.0,
  "reason": "仅供日志审计，不发送给买家"
}
```

`action` 只是候选计划，真实执行必须经过后端权限和状态校验。`intent_candidate` 不作为独立硬编码决策源。

### 6.3 回复原则

- 已知信息不重复询问；
- 一次优先补齐一个最关键缺口，能安全合并时再合并提问；
- 回复必须短、明确、可执行；
- 不用 Markdown 表格向买家展示内部字段；
- 交易事实由工具返回，Agent 只负责解释；
- 普通 FAQ 可以由 Agent 回答；涉及实时票价、场次、订单、出票、退款必须调用工具或转人工。

## 7. 会话、状态机、并发与人工接管

### 7.1 会话上下文

- 有订单：保留到订单生命周期结束，包括退款完成；
- 无订单：默认保留最近24小时；
- 上下文分为“已确认事实、候选信息、过期信息、买家原话”，只有前两者在满足条件时可参与报价；
- 不从任意历史消息推断数量、确认或退款授权。

### 7.2 订单状态

使用良票状态：`CREATED`、`SUBMITTING`、`TICKETING`、`TICKETED`、`SETTLED`、`REJECTED`、`FAILED`、`CANCELLED`、`REFUNDING`、`REFUNDED`。

状态冲突时按事件序列、订单详情和金额/票码等权威字段重建，不采用“最后一条消息覆盖一切”。

### 7.3 回调

必须处理并签名校验：`order.ticketed`、`ticket.updated`、`order.failed`、`order.settled`、`order.refunded`、`order.refund_rejected`、`recognition.finished`。回调在10秒内返回 `{"code":0}`，失败按良票重试策略处理。

### 7.4 人工接管

人工回复后，当前消息停止 Agent 自动回复；买家下一次新消息可恢复 Agent。以下情况必须创建人工任务：工具失败、金额不一致、多个订单无法唯一匹配、固定规则缺失、二次出票失败、买家争议或敏感售后。

## 8. 知识库与提示词统一要求

### 8.1 知识库分层

1. `fixed_flow`：固定交易流程和安全规则；
2. `api_contract`：良票接口、状态和错误处理；
3. `shop_config`：店铺级价格、时效、渠道和文案；
4. `faq`：普通咨询和服务说明；
5. `historical`：旧系统资料，仅供迁移审计，不得直接注入运行时。

运行时不能把所有知识条目无差别注入提示词。应按当前阶段、订单状态和问题主题筛选；历史条目若与本规范冲突，标记废弃。

### 8.2 必须删除或改写的旧意图硬编码

- 将大量买家原话硬映射为永久意图；
- 用“正确/可以/要了/嗯嗯”单独触发下单；
- 通过历史消息数量自动覆盖当前数量；
- 固定要求所有座位纠正重新发截图；
- 把所有 `order.failed` 都导向人工而不提供可配置的一口价切换；
- 固定回复“人工出票”而忽略 Agent 已获授权的自动工具流程；
- 通过关键词重写 Agent 回复并擅自插入座位、票数或价格。

保留的硬编码只能是状态、安全和接口约束，并且要能在日志中说明命中原因。

## 9. 当前源码中的已知冲突（历史审计清单）

以下是根据当前 V4 源码和已有文档整理出的冲突，不表示本次已经修复：

### P0：必须先修（已完成，保留作审计记录）

- `backend/app/prompts.py` 的权限提示仍禁止 Agent 创建订单、改价、出票、发货和退款；需改为“受控工具可执行，后端校验兜底”；
- `backend/app/chat_service.py` 仍是纯文本 completion，没有完整的工具调用循环；
- `backend/app/plugin_automation.py` 事件白名单缺少 `order.failed`、退款和票码更新等事件；
- `backend/app/rule_templates.py` 和 `reply_template_store.py` 仍大量使用“人工出票/联系客服退款”旧文案；
- `order.failed` 处理必须禁止再次调用 `/order/refund`；
- 需要新增 `STANDARD+FIXED` 失败切换状态和店铺级开关；
- `plugin-runtime/wanda-seat-autoquote` 需要给写动作加状态、幂等、权限和 `action_source` 校验。

### P1：核心体验（已完成，保留作审计记录）

- 删除或收缩 `_replace_redundant_followup` 一类基于关键词的回复重写；
- 将座位文本归一化放到独立解析器，并把已有上下文传给重新报价流程；
- 将知识库从“全量注入”改为按阶段检索；
- 修正任意历史文本被当成已确认票数的问题；
- 增加失败询问、买家同意、固定报价、重新拍下、付款、二次失败的状态机和超时任务。

### P2：性能与可观测性（持续优化项，非本次收口范围）

- 当前模型响应约11–22秒，发送约1秒，瓶颈在非流式模型生成；
- `chat_max_completion_tokens`、`chat_context_messages` 的配置持久化仍需核实，避免重启后恢复默认；
- 记录每轮：规则命中、Agent 开始/结束、工具耗时、发送耗时、最终动作和人工接管原因；
- 对同一业务动作使用幂等键，重试发送不重试出票/退款。

## 10. 最小验收场景

1. 买家只问“多少钱一张” → Agent 引导发截图或补齐影院/影片/场次，不抢先下单；
2. 买家发完整文字信息 → 能查询场次并报价，不强制截图；
3. 买家把 `9排的13 14` 改成 `第9排13、14` → 复用上下文重新预报价；
4. 报价后买家说“好的”但没有数量 → 继续询问张数；
5. 明确“确认，2张” → 只在有效报价和数量匹配后允许拍下/改价；
6. 特惠 `STANDARD+LIMIT` 失败 → 记录退款金额，询问是否换一口价，不调用 `/order/refund`；
7. 买家回复“可以”紧邻换渠道问题 → 重新预报价一口价，再要求确认金额；
8. 买家说“不换了”或提交平台退款 → 立即终止切换；
9. 一口价没有匹配规则 → 不自动报价，创建人工任务；
10. 一口价再次失败 → 不尝试第三渠道；
11. `TICKETED` 初始无票码但有 `pickupUrl` → 等待 `ticket.updated`，不报失败；
12. 工具超时/发送失败 → 只重试允许的层级，不重复创建订单或退款。

## 11. 最终锁定结论

- 固定流程必须完整执行，但固定流程不再等同于“全部人工”；
- Agent 允许直接回复并通过受控工具参与价格、订单和出票流程；
- AI 不产生交易事实，所有事实来自良票和后端权威状态；
- 大量猜意图硬编码删除，保留状态、安全、幂等和频控规则；
- 特惠失败可在店铺级开关开启时切换同场次一口价；
- 良票 API 文档优先于早期规划，尤其是：`FAILED` 已释放冻结资金，不再调用退款接口；
- 本文是后续代码、提示词、知识库、配置 UI 和测试用例的统一来源。

## 12. 当前实现状态

截至 2026-09-01，Agent 工具循环、良票查询接线、座位纠正、阶段知识库、LIMIT/FIXED 价格规则、回调状态路由、FAILED 禁止退款、快照候选确认和下单前新消息校验已在本地源码和自动化测试中完成，但尚未部署到生产。

会话策略页面现已接入记忆时长、消息条数、阶段门禁、介入范围和人工接管延迟；`business_background` 为正式字段，旧 `persona_background` 仅作兼容读取。

特惠失败后的固定切换必须保留买家同意、30 分钟有效期、拒绝/过期和二次失败转人工状态；任何状态未满足时不得自动创建 FIXED 订单。
补充说明（2026-09-01）：固定切换本地已闭环：`order.failed(LIMIT)` 持久化待关闭状态 → 闲鱼原订单关闭 command 执行并权威确认 `closed` → 再询问买家是否切换 → 明确同意后重新执行 `STANDARD+FIXED` 预报价 → 买家明确确认且带票数后才生成 `create_liangpiao_order`。过期、拒绝、unknown 或二次失败均禁止继续自动下单。本地自动化测试已通过，真实闲鱼/良票环境和生产部署仍待灰度验收。
