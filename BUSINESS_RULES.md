# V3 业务规则（唯一准则）

本文件是报价、识别、自动回复和订单处理的唯一业务规则来源。任何涉及会员价、选座、圈选、订单或买家文案的需求，必须先更新本文件，再同步更新实现与回归测试。

## 1. 不可变安全边界

- 系统仅做信息识别、万达实时核价、合规回复，以及受门禁约束的待付款改价。
- 价格唯一事实来源是**万达实时座位图中的原价**与临时试价订单锁座后万达官方 `mkt/activity/secret/list.api` 返回的唯一可用 W+ 会员专享优惠；截图中的价格、文字、手绘标记和历史报价均不得参与金额计算。截图中高置信度、明确可比的同座区单价或官方已选座总价只能用于“本店权威实时报价是否高于买家当前APP明确报价”的单向保护判断：若本店报价更高，不向买家展示或绑定本店报价，不允许其进入确认、下单或自动改价链路，只原样发送后台 `quote_buyer_app_better_price` 文案“您现在用的APP有合适的优惠价，可以自行购买。”；座区不一致、张数不一致、价格置信度低于0.85或价格不完整时不得比较。每个座位的最终单价统一按 0.1 元四舍五入；同价座位按单价乘张数，混合价座位按各座位轮整后价格逐座求和；订单改价必须使用同一权威总价。实时座位接口返回的 `channelFee` 只能按座位保存为有界诊断证据并汇总到该轮报价记录；在业务尚未确认其会计口径前，不得擅自把它加入报价公式、利润计算或买家回复，缺失时也不得猜测。
- 只允许为读取万达官方活动优惠创建短时万达临时试价订单：锁座、订单状态、活动优惠、取消和释放复核必须由V3通过账号池直接调用万达官方接口，不得调用出票系统的建单、优惠或取消接口；同一轮从创建订单开始必须绑定同一账号；创建响应只有在顶层 `code=0`、`data.bizCode=0` 且存在 `data.orderId` 时才可进入成功路径，随后订单状态还必须满足 `orderStatus=40` 且 `lockSeatTime>=0`，否则不得查询优惠；同场次串行，优惠查询后必须在 `finally` 中立即取消，取消后的订单状态必须同时满足 `orderStatus=60` 和 `lockSeatTime=-1`，并最多按即时、2秒后、再等5秒的有界节奏读取实时座位图3次，以容纳万达座位图的正常同步延迟；只有全部试价座位恢复可售才算释放成功。取消失败、释放无法确认或返回结果不唯一时禁止报价并转人工。同一场次、同一组座位在短时间内被重复临时试价时，万达可能已经返回取消状态但仍把第二次座位保持为不可选直到自动取消时间；取消接口成功或订单状态显示已取消都不能替代实时座位图释放证据，三次复查仍不可选必须以 `temporary_lock_release_unverified` 停止报价，不得沿用前一聊天或历史报价；失败后可以继续在后台约15秒、30秒进行最多2次只读实时座位复核，但原报价结果必须保持失败，不能补造成功、不能切换账号或继续试价，且原账号租约必须保留并按后台等待窗口续租到复核结束；续租失败时必须停止后台读取，防止与新的临时试价并发。服务优雅停机时必须有界等待正在执行的后台释放复核，超时也不得改变原报价的失败结果。不得为买家保留座位、不得支付该临时订单、不得把试价座位当作买家官方选座。除此之外，系统不得锁座、创建或取消订单、自动出票，也不得通过其他流程绕过平台改价失败。
- 影院必须先在只读官方影院库中召回唯一候选，或仅在影院名称候选存在有界歧义时，由官方影院候选与万达权威影片、绝对日期、开场时间组成的联合身份唯一消歧，再由万达实时接口成功返回实时座位图，才可继续核价；联合匹配置信度必须至少为0.85、场次候选数必须为1，影院名称不得冲突，影片、日期和开场时间必须精确匹配。若这些权威字段仍命中多个候选且截图清晰提供影厅，允许用官方场次影厅作确定性正向决胜字段；只有唯一一个候选的官方影厅匹配时才可继续，影厅不得覆盖影片、日期、时间或分店冲突。买家明确补充城市后，该城市是官方影院候选的硬边界；V3传入的已验证城市、影院、影片、绝对场次、影厅和官方座位提示必须覆盖轻量文本解析器的冲突猜测，解析器不得把影院地标（如“汉街”）反向覆盖明确城市；其他城市中恰好同影片、同日期、同时间的场次不得反向覆盖；截图明确提供影厅且官方候选影厅冲突时必须停止自动报价。SQLite 已唯一命中影院时必须直接使用其官方影院ID和城市信息，不得再把核价依赖于万达远程城市列表；远程城市接口超时必须归类为暂时性网关失败，不能伪装成影院、影片或场次不匹配。不得仅凭名称是否包含“万达”判断资格。官方影院库中的寰映、儒意等合作或历史品牌可进入实时核验，任一门禁失败均不得报价。猫眼、淘票票等第三方截图只可提供定位和官方已选座证据，价格仍只能来自万达实时座位图；不得把截图来源平台、影院库记录或别名本身等同于已核验场次。
- 已付款订单停止核价和改价；出票、圈位处理及售后均由人工完成。后台订单管理中的付款、待发货、已发货、交易完成和关闭状态必须从闲鱼权威订单镜像读取，不得用插件会话阶段替代。本店人工流程规定只有完成万达出票后才会点击闲鱼发货，因此闲鱼订单明确处于“已发货、待收货或交易成功”时可确定性展示为“已出票”；仍为待发货时只能显示“尚不能确认已出票”，不能反向断言一定未出票。订单读取失败时显示未知，不得通过后台假按钮猜测。
- 插件成功改价后，闲鱼自动插入的改价订单卡不是人工接管证据；仅当 `order.price.changed.operatorId=plugin:wanda-seat-autoquote`、权威订单总额与有效报价一致时，发送一次确定性“价格已修改”提醒可忽略该平台订单卡。真实卖家文字或图片回复仍必须触发人工接管；已付款订单不得补发历史付款提醒。
- 发货、假发货、催确认和评价能力在本插件中硬禁用，不得因未来误增权限而启用。
- 店铺总开关关闭后，识图、报价、AI 回复、改价及订单生命周期自动消息均停止；识图、报价、AI 回复和改价分项开关必须各自独立生效。
- 模板或可配置文案不得删除、弱化或覆盖以上边界；系统必须强制补足安全语义。
- 会话智能体可以理解意图、规划最少追问并请求白名单内的识图核价、W+座位查询、文字座位偏好、报价确认、订单状态或人工接管工具，但不得拥有可传金额或订单号的 `change_price`、创建订单、付款、出票、退款或发货动作；模型提供的金额、优惠和订单执行参数必须拒绝。`request_price_change` 只允许是不带金额、订单号或其他参数的“申请执行”动作；Shadow可以在系统已有关联订单、已确认且未过期报价时模拟工具选择，但事实必须保持 `price_change_requested=false` 且不得调用平台；Active和生产执行继续以 `agent_price_change_not_enabled` 硬拒绝。未来开放执行时也必须由确定性系统自行读取当前关联订单和已确认报价、重新执行全部门禁后决定是否调用平台改价；未达到独立评测门槛前不得解除禁用。工具返回的权威价格、座位、数量、有效期和订单事实不得被AI改写。当前生产影子智能体已使用独立持久化worker，采用最多8步的有界 Observe → Plan → Tool → Observation 循环，并按 `recognize_image` → `resolve_showtime` → `quote_realtime` 分离评估；每步保存检查点并使用独立60秒租约、每15秒续租，崩溃后可恢复。每个影子任务默认有3分钟持久化deadline；到期后必须记录为 `timed_out`，停止后续模型和工具步骤并纳入失败评测，不得无限续租。影子任务不得发送买家消息、重复创建临时试价订单、阻塞确定性回复或占用业务事件60秒租约；模型慢调用期间也不得被重复领取；未知行动、低置信度、工具失败、已付款或人工接管时失败关闭。上线必须先经过影子模式评估，订单改价继续由确定性事件链路执行。在Active尚未具备独立持久化执行所有权、幂等工具调用和回复outbox前，后端必须拒绝保存 `conversation_agent_mode=active`，并把任何历史遗留的Active值按Shadow读取，防止误触发同步Agent。Agent每次工具执行前必须持久化 `pending` 调用记录，完成后保存有界权威Observation；崩溃恢复时已完成调用只回放结果，结果未知的 `pending` 调用必须停止并转人工，禁止盲目重复。Active回复必须先写入独立持久化outbox；outbox只接受Active任务，以稳定 `action_id` 去重。独立单并发outbox worker发送前仍执行消息去重和人工接管门禁；成功后登记平台消息ID，人工接管则记为 `skipped`，异常或发送租约过期则标记 `unknown` 而不是自动重发。服务停止时必须主动中止仍在等待的Agent模型请求、将任务延后并释放独立租约，不得等待模型65秒超时或被systemd强杀。运行时设置必须明确返回 `execution_owner`；当前固定为 `deterministic`。即使配置文件遗留 `active`，只要owner不是 `agent`，插件也必须降级为Shadow并继续确定性链路，禁止双执行者。`create_manual_task` 必须写入按租户隔离的持久化人工任务队列，以事件派生的稳定任务ID去重；账号、会话、买家和订单关联只能由系统上下文注入，模型参数不得提供。新任务进入 `open` 并显示在异常复核，可打开原会话；只有认证后台操作可标记 `resolved`。Shadow模式仍不得真实创建人工任务。独立持久化Active Runner目前只允许保守白名单内的低风险流程咨询轮次进入：业务worker仅持久化Agent任务并立即完成原事件，模型只在Agent worker运行，最终安全回复写入outbox后由独立worker发送。图片、核价、有效报价、关联订单、付款、出票及其他交易轮次仍由确定性执行者处理。Active任务必须同时验证源事件的 `execution_owner=agent`，否则不规划、不调用工具、不入outbox。该能力在生产继续禁用，直至影子评测和真实工具链达到门槛。Agent Runtime提供无参数的 `inspect_ticket_request`，只返回图片存在性、明确张数、文字座位指令、关联订单、有效报价及场次身份字段完整度；座位坐标永远不作为张数。`read_linked_order`不接受模型提供的订单号，只使用系统关联订单读取闲鱼权威镜像，并仅返回 `unpaid/paid/shipped/completed/closed` 等有界状态，不暴露订单号、买家身份、金额或完整平台响应。订单读取失败、工具结果未知或Active运行无法安全完成时，必须幂等创建人工任务并通过outbox发送通用人工处理提示，不得向买家泄漏内部错误。Active Runtime的 `recognize_image → resolve_showtime → quote_realtime` 已接入真实权威工具：识图调用现有受控识图入口；场次解析使用独立只读接口，只执行官方影院目录和联合场次匹配，不读取座位、不锁座、不创建试价订单；完全相同的影院、影片、绝对日期、开场时间、影厅和官方座位提示可对正在执行的只读匹配请求做并发合并，并仅缓存完整唯一成功结果15秒、最多200项，负结果和部分匹配不得缓存；实时核价才允许进入既有临时试价、取消和释放复核链路。每步结果先写工具journal，识图与场次之间使用不向模型暴露的有界持久化artifact；服务恢复可从已完成工具结果继续，未知结果不得重试。Shadow仍只回放确定性结果，不重复真实临时试价。Agent权威报价回复必须自动附加“接受本次报价请回复确认”，并携带不向模型暴露的报价送达快照进入outbox。平台发送成功后，outbox先持久化平台消息ID为 `sent_pending_commit`，再由独立提交阶段将总价、张数、规则版本、场次、成本证据、圈选原图和平台消息ID幂等写入会话报价状态；提交失败只重试本地状态提交，绝不重发买家消息。发送结果未知或因人工接管跳过时不得创建有效报价。只有提交完成后状态才为 `sent`，报价有效期从提交时开始。Active图片路由在端到端评测前继续禁用。闲鱼账号返回 `E_IM_WS_UNAVAILABLE` 时，属于平台明确的未发送结果：业务事件应每10秒重试，最多保留约1小时，不使用指数退避拖延到数分钟，也不把事件在8次后永久失败。首次回复在平台发送失败时必须释放发送占用，连接恢复后可重新发送；不得把未送达的首次回复标记为已发送。该机制只能保证恢复后快速续跑，账号WS断线期间插件无法接收或发送IM，必须在鱼麦多后台恢复该闲鱼账号连接。Active Canary流量开关必须默认0%并启用总熔断；只有独立批准接口同时收到当前运行版本、1%～5%的比例，以及“安全审计就绪、图片离线评测就绪、唯一执行所有权已证明、一键回退已验证”四项明确证据时，才可保存批准状态。稳定分桶必须仅由租户和事件ID计算，未命中、版本变化、非低风险轮次或任一证据缺失都继续由确定性链路处理；撤销批准必须立即恢复0%和总熔断。批准状态不能自动解除后端对Active模式的独立硬禁用。Active Canary门禁必须基于至少100个独立自动安全审计样本；工具选择正确率至少95%，高风险动作、虚假声明、重复追问和权威结果不一致均为0。自动审计必须逐轮检查工具路径、完成状态、deadline、重复调用、高风险工具、权威结果一致性、最终回复安全性和模板占位符；失败或超时轮次必须计入分母并判为不合格，不得通过只筛选成功样本提高结果。运行版本或权威工具行为变化后旧样本不得计入当前版本。自动计算结果只表示技术门禁通过，不得自动修改Active配置，仍需独立执行所有权检查和显式Canary批准；不要求逐条人工审核样本。图片Active还必须对独立Shadow运行记录做离线工具链评测：至少100轮图片样本、Agent运行完成率至少95%、成功报价样本中 `recognize_image → resolve_showtime → quote_realtime` 完整路径通过率至少95%，且 `quote_realtime` 重复调用、未知工具结果、图片链路高风险工具调用和Agent/确定性权威结果不一致均为0。评测必须自动展示P50/P95端到端运行时延并对异常升高告警；时延暂不单独作为放行条件，不要求逐条人工确认。执行层必须维护场次解析阶段状态：未成功完成 `resolve_showtime` 时，`quote_realtime` 不得进入外部核价客户端；即使模型越级申请，也应以 `showtime_resolution_required` 失败关闭。

人工介入是受控监督信号。系统应使用独立后台扫描器识别 `source=external_seller` 的非插件卖家回复，并与其前一条买家消息及对应Shadow运行关联，形成按租户隔离、稳定哈希去重的“Agent对照评估草稿”。草稿只保存脱敏且有界的买家问题/图片存在性、会话事实、报价和订单生命周期、Agent计划与脱敏拟回复、人工实际回复及介入后的结果；不得保存完整会话或平台完整响应。系统可自动比较意图、工具、事实、风险和回复策略，但自动标签仅供审核，不得直接训练模型或自动修改规则。审核标签仅允许 `aligned`、`safer_than_human`、`missed_context`、`wrong_intent`、`wrong_tool`、`unsafe_claim`、`unnecessary_handoff`、`needs_policy`；人工审核后只记录拟沉淀目标：知识库、回复模板、业务规则或工具门禁，后续仍须进入对应受控发布流程。人工对照扫描不得阻塞买家业务事件。

消息合并等待可在2～60秒内配置。Webhook入队不得硬编码高于最小值的等待时间：入队先使用2秒最小窗口，业务worker读取权威租户设置后只在配置更高时补足剩余窗口。当前为降低体感延迟使用2秒，业务worker轮询间隔使用250毫秒以减少到期事件等待；识图、实时座位和临时试价仍必须等待权威结果，不得通过发送未经核验的价格来换取速度。全新会话的首张核价图片必须先持久化事件，再由业务worker独立领取并发送一次不含交易事实的“收到，正在核对万达实时场次和优惠”安全回执；该回执不得等待2秒交易事实合并窗口，发送后原事件必须返回正常合并窗口，识图、场次、座位、试价和最终回复仍不得提前执行。已有会话中的图片或明确核价补充在业务worker内连续处理超过3秒时，可以先发送一次确定性的“正在核对万达实时场次和优惠”进度提示；提示不得包含金额、库存、可售、锁座或订单承诺，最终权威结果仍只能发送一次。全新会话如果首条消息已经包含图片，不得再发送要求上传截图的首次流程说明；图片安全回执必须使用独立一次性占用标记，即使买家先发送问候、随后在首次会话窗口内发送图片，也允许独立确认已收到图片。安全回执发送失败必须释放占用以便重试；快速补充消息到达时，正在发送的回执事件可完成回执，但返回合并窗口后必须以更新事件为准取消旧交易处理。全新会话已发送首次流程说明时不得阻断首张图片的独立安全回执；同一会话不得重复发送该回执。进度提示属于当前买家事件的有界插件续发；即使最新出站消息是本插件较早发送的首次流程文字或附件，也不得被普通重复回复门禁误拦截，但真实人工出站仍必须阻断。Agent图片首步、已有Observation的续步以及包含图片、选座、核价、张数、订单、付款、出票、售后、确认或常见问候等明确票务关键词的轮次，属于受执行层严格约束的快速规划路径：关闭模型深度思考并将输出预算限制为400 Token；无图片、无Observation且不命中明确票务意图的首次复杂咨询仍保留思考能力和最多800 Token。快速规划只能选择既有白名单动作，不能绕过逐步Observation、阶段门禁或权威工具结果。

为降低买家仅补充张数时的重复识图延迟，确定性会话草稿可在10分钟有效期内保存不超过5KB的脱敏识图artifact。当前消息严格为“X张/个座位”且没有新图片时可以复用，系统只覆盖买家明确张数；买家在artifact有效期内重复发送完全相同的原始图片链接时，也可以复用该图片的有界识图artifact；同一闲鱼账号发送链接不同但SHA-256内容哈希和同轮文字输入完全相同的图片时，可在进程内复用5分钟内的有界识图结果，缓存最多100项并按租户、账号、图片内容、文字输入和识图端点隔离，不得保存原图、链接、Token或模型原文，但必须重新执行实时场次、座位、临时试价、取消和释放验证。不同的新图片、城市/影院/影片/日期/时间变化、文字座位或artifact过期时禁止复用，必须重新识图。artifact不得包含Token、Cookie、授权参数或模型原始回复，也不得发送给会话模型。

当模型越级申请 `quote_realtime` 但尚无成功的 `resolve_showtime` Observation时，执行层不得调用该工具，也不应立即终止整个Agent任务；应写入无副作用的 `policy_guard` Observation，明确下一步只能是 `resolve_showtime`，让Agent在剩余步数和原deadline内重新规划。错误申请仍保留在轨迹中用于工具选择评测，不能被自动纠正后隐藏。若模型继续越级、达到步数/deadline或前置工具失败，则失败关闭并转人工。图片识别已得到影院、影片、日期或场次身份后，即使模型被文字座位或历史确认状态干扰，也必须先完成只读 `resolve_showtime`，再进入唯一一次 `quote_realtime`；模型原始的 `quote_realtime` 越级申请仍按前述规则保留。离线评测必须单独统计被安全内核恢复的前置条件越级申请；恢复成功不能隐藏原始错误，当前版本越级重规划率必须不高于5%。历史真实会话可以用于加速当前版本评测，但必须由当前 `runtime_version` 创建独立、版本化、稳定哈希去重的 `evaluation` 运行，逐步重新执行Agent规划并只回放当时已持久化的确定性权威Observation；不得读取该会话后来形成的付款、订单、报价或人工接管状态来污染历史时点判断；不得发送买家消息、创建人工任务、再次调用真实识图核价、临时试价、改价或其他外部副作用。同一源事件无论有实时Shadow和历史回放多少份都只能计为一个独立样本。历史回放必须低优先级、小批量补队，当前Active/Shadow买家轮次永远优先；历史数据不得作为当前实时价格、座位、账号额度、执行所有权证明或Canary稳定运行时长的替代证据。

买家在自然长句中明确提供“省＋市＋区县＋分店万达/万达影城”时，文字事实提取必须支持从句中提取城市和分店关键词，例如“山东省淄博市张店区富力万达的万达影城”应形成城市“淄博”和影院候选“富力万达”；这些只作为官方目录和联合场次匹配输入，最终影院名仍以只读官方匹配结果为准。`W+`、`w+`、会员等紧随尚未形成有效报价的截图补充消息属于报价补充，应在图片有效窗口内触发同轮重新处理，而不是普通闲聊。已有送达且未过期的有效报价后，买家只问“多少钱”“这个呢”“不是X元吗”“会员价可以优惠吗”或“这个金额直接拍下是吧”时，必须通过 `read_active_quote` 或确定性同义快速分支直接回放当前权威报价和确认/禁止直接付款提示，不得重新识图或再次创建临时试价订单；买家说“这两个座位/位置”只表示对前文文字座位的指代，不得触发识图、库存判断或重新核价。新增功能上线前必须保留这些已有报价后快速分支的回归测试。

Active放行评测必须按Agent运行版本隔离。新运行时版本上线后，只统计携带当前 `runtime_version` 的样本；旧版本样本仅显示为历史数量，不得污染或补足当前版本的100轮门槛。
- 每个Agent工具必须声明可观察副作用等级、允许返回的事实字段、允许建议的下一步及是否允许权威回复。执行层必须在工具返回后验证该契约；出现未声明字段、越权下一步、工具身份不一致或越权权威回复时，以 `agent_tool_contract_violation` 失败关闭，违规内容不得进入模型上下文。具有持久化写副作用的工具在非Active模式必须在执行前阻断；仅允许由Runtime明确标记的Shadow/Evaluation模拟工具返回全为否的无副作用观察。工具契约变化必须升级 `runtime_version` 并重新积累当前版本评测样本。
- 后台Agent评测必须提供按租户隔离的单轮轨迹回放，联合展示脱敏买家轮次摘要、每步计划、工具journal状态与耗时、有界Observation、策略守卫Observation及最终权威结果。回放不得返回完整事件payload、完整聊天、内部识图artifact、Token、长订单号、手机号或图片链接；它只用于审计，不能重放工具或发送买家消息。
- 持久化人工任务除 `open/in_progress/resolved` 状态外，可保存负责人、`low/normal/high/urgent` 优先级、最多8个短标签、SLA截止时间和最多20条有界内部备注。全部更新必须租户隔离并记录操作者；内部备注不得发送给买家，任务完成后不得重新打开，任务字段不得触发改价、出票、退款或发货。
- Agent知识库的已审核回复知识必须先通过确定性场景分类再注入。场景仅限通用、接待资料、报价后追问、下单待付款、付款出票和售后；每轮只允许当前场景与通用知识，场景知识优先，并同时限制条数和总字符数。知识场景不得覆盖权威价格、座位、场次、订单或付款工具事实；历史无场景条目按通用知识兼容处理。
- 影子与启用模式可以提炼会话经验，但只能以经平台消息ID确认“非本插件发送”的卖家回复及其后买家明确理解、感谢或继续提供资料为证据；这类回复可能来自人工或其他已接管工具，不能未经审核视为正确答案。只允许提炼问候与结束语、图片要求、服务范围、服务流程和沟通方式等低风险经验；候选中禁止数字、金额、价格、优惠、订单、付款、改价、库存、座位可售、出票、发货、退款、联系方式、链接和个人信息。经验必须按租户隔离、内容哈希去重并累计一致证据，只保存泛化问题、回复策略、参考表达、结果信号和置信度，不保存完整会话或模型原始输出；新经验一律为停用草稿，只有人工审核并启用后才能进入后续提示词。
- `record_seat_preference` 是兼容保留的内部动作名。对圈选、红点、绿点或手绘标记，它必须保存为“按买家原图圈选位置出票”的履约指令，而不是“出票偏好”：系统无需识别、生成或向买家复述具体座位号，人工出票时直接查看买家原图并按圈选位置操作；若圈选位置届时不可选，必须停止并联系买家确认，不得擅自换座。该指令不等于官方已选座，不锁座、不承诺余票，也不得改变权威报价。文字座位仍不得被提升为官方座位事实。
- `confirm_quote` 只能表示Agent无参数请求确认，不得携带金额、订单号或自行裁决确认结果。Shadow/evaluation只能模拟且不得写会话；Active也必须调用V3确定性会话门禁。确定性系统仅在实时核价成功、张数明确、报价未过期、价格规则版本明确且完整报价回复已成功送达并登记平台消息ID时，才能生成可关联订单的有效确认报价；任一条件缺失必须拒绝并要求最新完整选座页或转人工。
- `get_manual_task_status` 必须是无参数只读工具，只能按系统注入的租户、店铺账号、会话和买家地址读取该会话最新人工任务；返回给模型和买家的事实仅限是否存在及 `open/in_progress/resolved` 状态，不得暴露任务ID、订单号、负责人或内部备注。`resolved` 只表示人工处理记录已更新，不等于已出票；出票仍以人工明确通知和闲鱼权威订单状态为准。历史evaluation缺少事件时点任务快照时必须失败关闭，不得读取当前任务状态污染历史评测。
- `show_available_wplus_seats` 必须是无参数只读工具，排数只能从当前买家文字中确定性提取，影院场次只能来自已持久化识图artifact；缺少明确排数或场次artifact时失败关闭。Active只能调用V3只读场次匹配和官方实时座位接口，不得进入临时试价、锁座、活动报价或订单接口；返回模型的事实仅限请求排数、最多30个规范座位号、可用数量、W+优惠标记和规范影院名，不得返回价格、Token、账号或完整官方响应。回复必须说明座位状态可能变化，不得承诺库存。
- 影子评测不能只保存意图、置信度和建议动作。每个可评测轮次必须形成最小化工具选择评测记录，由独立自动审计器校验“Agent实际工具路径、是否违反前置条件、是否异常追问或转人工、建议回复是否安全、与最终权威报价/订单结果是否一致”；失败、超时和缺少权威回复的轮次必须保留并计入分母。评测记录不得保存完整聊天、模型原文、密钥或敏感买家信息，也不要求运营人员逐条人工审核。只有工具选择正确率至少95%、高风险动作0、虚假声明0、重复追问0且权威结果一致性通过时，才可开放对应风险等级的 `active` 能力。
- 场次候选多于1个时使用 `showtime_not_unique` 并只追问真正缺失的身份字段；官方场次候选为0且影院、影片、日期、开场时间已经齐全时使用 `showtime_not_found`，要求买家刷新万达选座页后发送最新截图，不得重复要求补充已经提供的影院和开场时间。
- “什么时候出票、是否发货、已拍下待付款”等订单或履约语句不得触发重新核价；有关联订单时只读取权威订单状态，无关联订单时转人工。“红点、绿点、圈出或画出的位置”记录为按买家原图操作的出票指令，不要求识别具体位置；不得声称可售或重新解释为官方选座。感谢、评价、收货等结束语不得触发核价。
- 报价记录只保存权威报价、影院、场次、报价范围、实时成本、会话阶段、订单关联和最小化结果，不保存完整聊天、截图标价或敏感个人画像。同影院、影片、日期、开场时间和影厅的下单成功率定义为“已关联平台订单的权威报价数 / 已送达权威报价数”，付款成功率定义为“金额核验一致并进入人工出票的订单数 / 已送达权威报价数”；样本量和分子必须同时展示，不能用小样本百分比误导。当前报价记录只用于匿名统计，不训练模型、不自动推导或修改报价公式；后台展示的公式必须明确标识为商家当前设置。未来若独立实现AI价格建议，只能使用影院、场次、实时成本、供需、报价范围和成交结果等非个人因素，至少积累100条后才可由人工决定是否进入影子评估，并须在成本下限、利润边界、实时原价封顶和买家APP低价保护内运行；不得按买家身份、历史议价能力、聊天风格、支付意愿或其他个人特征差别定价，也不得绕过确定性安全规则。
- 回复模板允许附加一张5MB以内的PNG、JPEG或WEBP图片；图片必须上传到受控存储并保存HTTPS链接，后台必须显示完整链接。使用模板时先发送文字，再发送附件图片，两条平台消息ID都要登记；文字已发送但图片失败时不得重发文字。同一闲鱼账号重复发送同一受控模板图片时，可在进程内短时复用已上传的平台图片地址，避免每次重复下载和上传阻塞后续买家事件；缓存必须按账号和原始受控图片链接隔离、限制数量和有效期，发送前仍执行人工接管门禁。模板附件不得包含Token、Cookie、票码或买家敏感信息，也不得改变报价、库存和订单事实。

## 2. 报价计算规则

- 每次报价均使用当前万达实时座位图中的实时原价、同轮临时试价订单 `available-offers` 返回的优惠价和后台当前报价规则。
- **W+会员专属优惠价**：必须来自临时锁座后 `available-offers` 中唯一、可用且名称明确为“W+会员专享优惠”的活动整单价；允许周五会员日活动仅由独立开关控制。只读座位图的 `wPlusActivity.price` 仅作诊断，不再作为最终报价依据；不得用截图价格补全。
- **W+专享座价**：仅表示实时座位图中 W+ 专享区域的 `salesPrice`；它可能与原价相同，不等同于会员价或会员专属优惠，也不证明该场存在 `wPlusActivity`。
- W+专享区域即使没有 `wPlusActivity.price`，只要临时锁座后的 `available-offers` 返回唯一可用的 W+会员专享优惠价，仍可按后台规则报价；若优惠缺失、不唯一、金额无法按试价张数整除或释放未确认，则禁止应用负数调整并转人工。
- 识别与实时核价完成后合并为一条最终买家回复，不再要求买家先确认识别信息再调用价格接口；仅当已有会话的本轮权威处理超过3秒时，允许按延迟规则提前发送一次不含交易事实的进度提示。合并回复必须展示影院、影片、影厅和场次；只有官方已选座路径展示座位号，截图价格仍不得展示或参与计算。若有效的未选座区域试价之后收到官方已选座截图，必须以具体座位逐座核价替换旧试价；金额不同时明确告知“上一版未选座试价已失效”，并清除旧确认状态，买家须重新确认新报价。
- 图片只有三种受控路径：
  - **官方已选座**：每个官方座位必须在实时座位图逐座命中。任一截图已选座当前不可选时，回复必须明确要求在购票平台重新选择当前可选座位并发送最新完整截图，不得错误要求补充已经识别完整的影院、影片和时间，也不得在没有真实人工任务时声称“已转人工”。确定性核价失败回复属于权威安全结果，后台可编辑旧模板和模板图片不得覆盖该轮权威失败回复。同一座区的普通/优选/特惠等非 W+ 官方座位必须用这些具体座位创建临时试价并读取其唯一 `available-offers` 会员价，再加 `regular_adjustment_cents`；不得因为实时 W+ 座位为 0 而要求无关 W+ 探测座位或拒绝该非 W+ 官方选座。混合座区不得把多区域优惠总额平均成统一会员单价。W+专享座必须存在实时会员价，当会员价不高于 `wplus_member_price_threshold_cents` 时按 `max(实时原价 + wplus_adjustment_cents, 实时会员价)`，高于阈值时按实时会员价，并继续受会员价下限、原价上限和十分位轮整约束。逐座求和得到唯一权威总价，允许不同区域和不同单价。回复展示官方座位号。
  - 若真实会员优惠加后台调整后高于对应座位实时原价，但按 0.1 元轮整后的实时原价仍不低于同样轮整后的 `available-offers` 会员价下限，则直接以实时原价作为最终单价；只有会员价下限已经高于实时原价上限、无法同时满足“不低于成本且不高于原价”时，才以独立 `quote_price_conflict` 失败关闭并转人工。不得误报为识别失败或“W+优惠不可用”。
  - **仅标记需要购买的位置**：标记作为“按买家原图圈选位置出票”的履约指令，不得推断官方座位或张数；按买家明确张数从同场实时可用 W+ 专享区域试价。系统无需知道、展示或推断具体座位号，必须询问需要张数并请买家确认原图是否已圈出购买位置；人工出票必须直接查看原图并按圈选位置操作。若圈选位置届时不可选，停止并联系买家确认，不得擅自换座。
  - **既无官方选座也无位置标记**：按买家明确张数从同场实时可用 W+ 专享区域试价。回复不展示座位号，并询问需要张数及原图是否有购买位置标记。
- 后两种路径必须通过临时试价订单获得唯一可用的 W+会员专享优惠；买家已明确张数时按该张数临时试价，未提供张数时只临时试价 1 张并追问张数，不得用标记数量推断。
- 官方已选座任一座位无法实时命中时，必须返回 `official_selection_unverifiable` 并转人工；**不得**降级为 W+ 区域试价。截图出现“确认选座”按钮但首轮识别漏掉底部官方 `X排Y座` 卡片或底部清晰影片名时，可增加一次只针对底部场次/选座卡片的受控视觉复核；复核只能补充首轮缺失的影片名，不得覆盖首轮已有影片；只有复核输出的每个座位都符合明确座位标签格式，才能恢复官方已选座，地图上的绿色座位格本身仍不能作为座位号证据。
- 未官方选座不得绕过 `wplus_adjustment_cents` 或 W+ 会员价规则。
- 区域试价仅可从实时座位图中标记为 **W+** 的可用区域取样；普通、特惠或优选区域即使返回 `wPlusActivity`，也不得替代 W+ 区域试价。
- 区域试价只在优惠查询期间短时占用试价座位，回复前必须取消并确认恢复可售；取消成功后允许在有界时间内最多重读实时座位图 3 次以容忍万达释放状态传播延迟，任一次确认全部试价座位恢复即可继续，全部复核仍未恢复则失败关闭；不为买家保留座位；实时 W+ 可用座位为 0 时不得继续锁座或报价，使用后台模板明确告知“当前场次没有W+位置可选择，您看到的位置可能是维修状态无法购买”；优惠缺失或释放无法确认时不猜价格，转人工复核。

## 3. 选座证据等级

| 证据类型 | 可以做什么 | 不可以做什么 |
| --- | --- | --- |
| 官方已选座卡 | 按实时座位图精确核验官方座位和张数；可按受控临时试价流程读取优惠并立即释放 | 为买家保留座位、支付临时订单、承诺库存或自动出票 |
| 手绘圈出的座位位置 | 作为人工出票依据，由人工按买家标记的位置出票 | 视为官方选座、自动解析具体座位号、报价区域、张数、锁座、承诺库存或猜测具体价格 |
| 文字座位号 | 仅作沟通和人工参考 | 视为官方选座、价格依据或锁座依据 |

- 手绘圈不是平台官方座位。圈出位置无法唯一关联到实时 W+ 区域时，必须人工复核，不得猜价。
- 张数以买家明确文字或可核验的官方已选座为准；圈图数量仅供人工参考。“多少钱一张”“一张多少钱”“每张价格”等是在询问单价，不代表买家只需要1张，不得覆盖官方已选座张数或产生 `ticket_count_conflict`。已有完整报价后，买家明确改成不同张数时必须立即使原报价失效并要求对应张数的官方已选座截图；`order.created` 必须再次扫描报价后的明确张数，发现冲突时禁止确认、禁止改价并提示不要付款。
- 手绘圈、颜色和座位图视觉状态不参与报价判断；视觉识别仅判断是否存在清晰官方已选座卡片，并提取场次定位信息。手绘圈本身保留给人工出票使用，但系统不需要也不得自动推断其具体座位号。
- 在场次已唯一匹配且有效报价仍在时，买家明确询问某一排是否可选，可以只读调用万达实时座位图并列出该排当前可用的 W+ 座位号；回复只展示可选座位列表，不再要求买家回复座位或提示重新复核价格。该列表不等于官方选座、不锁座、不承诺库存，也不得据此把买家文字偏好改写成官方已选座。买家后续发送文字座位仅作为人工出票偏好，不重新识图、查询座位或核价。
- 视觉模型的常见 JSON 格式、空值、别名和枚举偏差可由受控归一化层修复为 Schema 安全默认值；可从带 Markdown/说明文字的模型文本中提取唯一 JSON 对象，未知座位区域降级为“未知”且不得参与自动定价。归一化不得补造影院、场次、价格、官方座位或张数。仍无法校验时才转人工，并记录稳定失败码。
- 视觉兼容回归样本仅可在受控本地测试目录保存，使用随机样本 ID 与 SHA-256，不写入代码仓库、不保存买家 ID/昵称、不用于生产回复或报价；样本访问与保留期须受限。
- 不保存模型原文、Token、完整平台响应或买家敏感信息。场次匹配诊断最多记录经长度限制的城市、影院、影片、日期、开场时间、影厅和结果数量；不得记录截图、买家原文、座位 ID 或账户信息。
- 脱敏报价预览仅用于短期人工复核，最多保留 24 小时且最多 500 条；过期记录不得继续显示为待处理任务，并在后续读写时自动清理。后台“异常与记录”必须提供可操作的脱敏诊断：核价失败码与步骤、改价前后金额、涨降方向、HTTP状态、受控上游原因和人工接管状态；业务拒绝即使事件已成功ACK，也必须在运营视图标记为执行失败，不能显示成“已完成/无错误”；不得展示完整买家消息、Token、Cookie或完整平台响应。AI 回复不建立“待审核回复”队列、不持久化建议回复；插件后台不提供会话中心、历史对话读取或手动 AI 模拟功能。
- 会话中的买家文本和普通图片 URL 最多保留 24 小时，过期报价草稿同时删除；订单状态、金额审计及人工出票状态可以继续保留，但不得连带长期保存买家原文。已付款待人工出票所需的确认图可保留到出票完成或进入售后。
- 人工按买家手绘标记的位置出票；手绘圈不影响实时报价规则，余票以人工出票时为准。

## 4. 客服目标链路

```text
事件 → workflow 状态机 → Agent 输出意图/工具候选 → 规则门禁 → 受控工具
→ 写入同一会话状态 → 模板/Agent 草稿 → 发送安全校验 → 自动发送或人工接管
```

- 新买家首次进入全新会话时，在店铺总开关和 AI 回复开关均开启的前提下，仅发送一次可配置的固定流程说明：发送完整选座页与张数 → 接受完整报价后回复“确认” → 按提示提交订单并保持待付款 → 仅在收到真实“价格已修改”提示后付款。首次图片仍须继续核价，不得被流程说明吞掉；但首次纯文字缺信息时，固定流程说明已覆盖截图和张数要求，不得紧接着重复发送缺信息模板。后续买家补充影院、场次或张数后产生的新一轮有界缺失信息回复，可以接在本插件较早的首次流程消息之后发送；相同文案仍去重，真实人工出站仍阻断。
- 只有同时包含张数和总价的完整实时报价才能进入订单流程。首次直接得到完整报价时附加固定确认指令；单价试价或待补张数状态不得诱导买家确认或付款。若系统已发送权威实时单价并明确询问张数，买家随后回复明确张数，则该回复同时完成总价计算和报价确认；系统必须发送单价、张数、合计以及“提交订单后先不要付款、等待系统改价、仅在收到价格已修改后付款”，成功送达后才允许新的未付款订单进入自动改价门禁。
- 已送达且未过期的单价试价收到“多少钱/你这多少/这个多少”等同义追问时，必须直接回放当前权威单价并询问张数，不得重新识图或重复临时试价。买家对有效完整报价明确回复“确认/OK/好的”等确认词后，必须发送后台 `order_submit_before_payment` 原样文案，并发送约定的“关闭免密、普通支付、确认购买后返回等待改价”操作示意图；若该模板已配置自定义附件图，则使用自定义图并不再重复发送内置示意图。文字和图片都必须通过插件自身发送并登记平台消息 ID，避免后续改价门禁把插件自己的引导误判为人工接管；若确有人工或其他插件近期出站消息，仍必须停止自动发送和改价。买家已经发送“已拍下/待付款/改价”等订单意图时只发送文字，不重复发送操作图。
- Agent 可以请求受控的 `send_customer_message`、`change_order_price` 等工具；规则门禁通过时工具可自动执行。Agent 不持有平台原始凭据，也不得绕过会话归属、订单状态、金额、幂等、人工接管和已付款门禁。买家业务订单的出票、退款和取消仍仅人工；临时试价订单只能由报价服务在 `finally` 中自动取消并确认释放。
- `workflow` 是唯一编排入口；识图、核价、订单和回复不得各自独立改变会话状态。平台交易通知和运营横幅必须在读取历史、识图和AI规划前过滤，包括收货、评价、发货、退款关闭、小红花及“不要被砍价/message_no_bargain”等平台内容。
- webhook 必须在平台 5 秒投递时限内完成验签、持久入队并 ACK；识图、官方匹配、临时试价和回复发送只能由持久化 worker 异步执行，不得阻塞 webhook。买家消息默认合并等待为 2 秒（后台允许 2～60 秒）；首条为纯文字时固定提示的发送可与独立视觉请求并行，首条已经包含图片时不得发送截图教程。一次核价轮次确需联合两张近期截图时，两张图片可并发执行只读识图，再按原消息顺序确定性合并身份字段；识图并发必须有全局有界上限，缓存或并发合并只能减少重复识图，不能复用价格、库存或订单结果；官方选座证据仍只允许来自最新权威选座图。最终报价仍必须等待视觉、实时核价、座位释放和发送门禁全部完成。平台最多重投 6 次，因此同一平台 `messageId` 必须跨事件 ID 幂等；迟到旧消息不得覆盖更新消息中的张数或座位事实。同一会话串行，不同会话允许并行。会话一旦关联活动订单或进入改价异常，不得再因买家发送“我已拍下、待付款”等订单消息重复发送提交订单提示。运营日志必须把缺失信息、场次待确认和官方已选座暂无法逐座核验显示为“待买家补充”，把非万达影院、张数冲突和无可售座位显示为“安全停止”，只有网关、账号、临时订单或释放复核等技术异常才显示“执行失败”；核价安全结果不得被后续回复的“重复回复”或“人工接管”状态覆盖。
- 买家在图片后发送普通闲聊不得取消待处理图片；只有新图片或明确报价补充信息可以合并替代同会话待处理轮次。买家在30秒内连续发送影院详情页和选座页时，应作为同一轮最多两张图片分别识别：最新的合格选座页是官方座位、区域及价格证据，较早的影院详情页只能补充其清晰可见且不冲突的城市、规范影院、影院地址提示、影片、日期、场次和影厅；不得把两张图中的座位或价格混合推断。没有有效报价时，买家仅回复“好的、收到、谢谢”等确认性闲聊应保持静默，不得调用自由回复模型，更不得声称“正在核对票价/库存、稍后报价”。只有本轮真实启动并完成实时核价后才能发送价格结果。
- 图片后的明确城市补充（如“广州”“常州”“德州万达”）可继承最近图片，并且只能作为影院/场次唯一匹配提示；不得修改官方座位或任何价格事实。SQLite 仅在城市与影院组合唯一时校正名称，仍不唯一则继续安全失败。完整文字场次允许紧凑格式，例如“河南郑州 中原万达 9.23奥德赛9:30-12:22 9排18座”；必须拆分为城市、影院、日期、影片、开场时间和文字座位偏好，文字座位仍不得冒充官方已选座。
- 追问必须只询问当前真正缺失的最少字段；截图或本轮会话已明确的影院、影片、日期、场次和影厅必须保留，不得要求买家重复提供或重发截图。影院名称中的品牌前缀、括号和 IMAX 等格式词只能用于官方库唯一匹配归一化，不能生成新影院。品牌、县级市/分店词和格式词顺序不同（如“万达影城（常熟IMAX店）”与“常熟万达广场店”）时，只能在去除品牌与格式噪声后对分店词做全库唯一匹配，命中多个候选仍须失败关闭；归一化不得因去除候选城市前缀而让缺少截图明确分店词的通用影院名通过后缀包含误匹配，例如“云龙万达广场激光IMAX店”不得误匹配“泉州万达广场激光IMAX店”，“万达广场IMAX激光店”这类只有品牌、广场和格式词而没有城市/分店身份的标题不得被校正成任一具体影院；窄屏标题末尾显示为 `I…/IM…/IMA…` 时，只能去除这个末尾不完整的 IMAX 格式词，不得截断中文分店名。若窄屏标题包含 `.../…` 且首次权威联合匹配已经返回唯一官方影院名、但因截图标题截断未能通过场次身份门禁，可以仅用该唯一官方影院名重试一次只读场次匹配；城市、影片、绝对日期、开场时间、影厅、张数和座位证据必须保持不变。未返回官方影院、官方影院不唯一、买家明确城市冲突或重试仍不唯一时必须失败关闭。截图同时清晰展示影院地址时，可将地址作为本轮瞬时官方目录匹配提示；只有地址与官方只读库唯一命中才能校正规范影院，地址不得写入报价诊断、会话草稿或长期存储，也不得使用买家收货地址。买家补充“浦东陆悦天地”等带有广场、天地、中心或店名的分店关键词时，可像出票系统一样进入官方影院库模糊检索；只有唯一命中才能补全规范影院名。截图明确为非官方目录影院且不含支持品牌时，直接使用后台“非万达影院”模板告知暂时只代订万达影院，不再要求补充万达场次信息。
- 受控工具只能返回结构化事实/错误码；规则门禁决定是否执行、是否发送和是否转人工。
- V3临时试价不得请求出票系统的建单、优惠或取消接口。账号池只读加载在线、风险正常且具备W+资格的账号；当账号事实明确给出整数 `remaining` 时必须大于0，明确为0或类型异常的账号不得参与试价；`remaining=null`或字段缺失只能表示额度未知，不能声称已预留额度，但仍必须依靠账号租约禁止并发试价；账号引用对外必须不可逆脱敏，Token、手机号和完整万达响应不得进入日志、异常或报价结果。每次官方临时试价成功后必须使用独立服务端密钥对稳定账号标识生成HMAC并截取32位 `pricing_account_ref`；该密钥不得复用账号Token，Token轮换不得改变引用。引用只作为内部报价证据随报价、outbox和会话报价记录传递；不得返回原始 `account_id`，不得把该引用发送给模型、买家或未认证的前端报价接口；只有通过服务端密钥认证的内部报价接口可以传递该证据。凡 `pricing_source` 表明来自万达临时锁座 `available-offers` 的报价，缺少合法 `pricing_account_ref` 时不得确认报价或执行平台改价，必须失败关闭并提示买家先不要付款。账号必须使用有界租约，同一账号不能并发试价；顺序请求应轮转账号。只有在前一账号的临时订单已经取消且座位已由万达官方实时座位图确认释放后，才允许因缺少可用W+活动而尝试下一个账号，单轮最多3个账号；创建结果未知、取消失败或释放未确认时禁止切换账号并立即失败关闭。万达官方域名必须使用严格HTTPS白名单，直接客户端不得包含付款、出票、退款或发货能力。影院/场次匹配与试价前的只读座位解析目前仍可通过票务后端只读接口，但不得由此绕回订单接口。
- 后台必须显式区分客服 Agent 接管、会话观察和会话经验提炼，不再把“模型自主训练”展示为可配置能力：Agent接管状态显示是否已由智能体管理低风险流程；会话观察显示保留事件中的智能体轮数及失败轮数；会话经验显示草稿、人工审核启用数量和累计一致证据。不得把报价统计或影子观察描述为AI已自主学习。未达到影子评测上线门槛前，后台不得允许选择 `active`；AI设置每次保存后无论PUT是否返回成功，都必须GET回读权威设置并逐字段确认，后端已确认写入后发生的页面渲染异常不得误报为保存失败。

## 5. 订单状态机与回复模板变量

### 订单状态机

- `待核验/待补信息`：可识别、最小化追问或按实时规则试价；除受控的“临时锁座 → 查询优惠 → 取消并确认释放”外，不得锁座或建单。
- `待付款`：仅在收到 `order.price.changed` 后回拉权威订单，并确认真实订单金额等于有效确认报价时进入；`order.created` 或改价提交本身不得提前写入。平台改价失败不得绕过；不得自动引导按过期价格付款。
- `E_OAUTH_FLOW_FAILED` 是平台上游调用的通用包装错误，不能仅凭名称断言为插件 scope 或 OAuth 授权失败；若响应中存在受控业务原因码（如 `CANNOT_MODIFY_FEE`）须优先保留并按业务拒绝处理，否则归类为 `PRICE_CHANGE_UPSTREAM_REJECTED`。只有 HTTP 401/403 或明确 `E_UNAUTHORIZED`/`E_FORBIDDEN` 才归类为 `PRICE_CHANGE_AUTHORIZATION_FAILED`；HTTP 401/403、明确授权失败和无具体原因的上游拒绝均不得自动重试。`order.created` 刚到达时的 `CANNOT_MODIFY_FEE` 可能是订单写状态尚未同步：只允许在 2 秒、5 秒后各重试一次，并且每次重试前必须重新读取权威订单、重新执行未付款、金额、数量、报价有效性、店铺归属和人工接管全部门禁；任一门禁失败立即停止，第三次仍拒绝才提示买家先不要付款并转人工，不得触发整个事件的外层重复写。改价失败审计必须最小化保存改价前权威订单总额、目标总额、当前邮费和增价/降价方向，不保存完整订单或平台响应。
- `已付款`：停止核价和改价；只有该会话曾形成插件权威报价时，才校验有效确认报价和权威实付金额。金额一致时进入人工出票，不一致、缺失、未确认、已过期或订单读取失败时进入异常人工队列。完全没有插件报价事实的其他订单归类为“非插件报价订单”并静默忽略，不得制造异常、通知买家或尝试改价；任何已付款订单都不得追溯修改价格。
- 已交易成功、关闭、取消、退款完成或已进入售后的历史订单不得永久阻止同一会话的新图片核价；发现具体关联订单但无法回拉确认其终态时仍应安全阻断。
- `人工出票/售后`：仅人工处理，不由系统自动出票、发送票码、发货、取消或退款。后台只允许人工在实际操作完成后回写“已出票”“票码已发送”或“出票异常”，记录时间与状态审计；同状态重复提交必须幂等，且“待人工出票”不得直接跳到“票码已发送”。不得保存票码内容，也不得因状态回写自动发送买家消息或调用平台发货。

### 回复文案

- 后台“回复文案”必须管理**全部**自动发送给买家的固定文案：报价成功、缺图/缺信息、识图结果提示、W+可用座位为0、核价失败与人工复核、非万达拒绝、订单已付款及订单流程提示；不得保留插件端或后端的买家可见硬编码回退。AI 自由生成回复不属于固定文案，但仍受本规则约束。后台多行模板中的换行必须原样传到平台消息，不得被空白归一化为一行。
- 后台“回复文案”只影响新消息；不得改变已确认报价或已付款订单。插件处理每条新消息时必须使用后台接口返回的当前模板，不得因运行时字段过滤而回退到插件内旧文案。
- 可配置模板变量仅限：

  ```text
  {影院} {影片} {日期} {场次} {影厅}
  {张数} {单价} {合计} {座位类型} {价格来源}
  ```

- 保存模板时：最大长度 500；拒绝未知变量；禁止静态写入价格、库存、座位 ID 或订单金额承诺；支持恢复默认文案。模板键必须固定、完整且在保存时整体校验，避免某条自动回复回退到硬编码。
- 模板必须由安全渲染层使用实时已核验数据填充；不得出现锁座、库存、座位号、出票承诺或价格来源以外的未核验事实。区域试价回复仅输出已核验的单价，以及已知时的张数和合计；不得加入手绘圈或技术流程说明。
- 买家以“9排18-19”“9排18、19”等文字明确列出多个座位时，可以确定性提取其数量作为区域试价张数，但这些文字座位仍仅是人工偏好，不得写成官方已选座、逐座库存或逐座价格已核验。单个座位坐标中的座号绝不能解释为张数，例如“7排15座”表示一个文字座位偏好而不是15张，“6排16”必须归一化为“6排16座”且不得触发整排查询或重新核价。
